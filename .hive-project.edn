{:project-id "hive-claude"
 :parent "hive"
 :description "Claude Code terminal backend — hybrid cljel + JVM addon for hive-mcp. Emacs-side bridge (cljel) + JVM ITerminalAddon registered as :claude"
 :language :clojure
 :tags ["claude" "emacs" "cljel" "terminal-addon" "ling" "addon" "hybrid"] 
 :carto {:source-roots ["resources" "src" "src/clj" "src/cljel" "test"]}}
